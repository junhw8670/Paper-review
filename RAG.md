# **Retrieval-Augmented Generation for
Knowledge-Intensive NLP Tasks**
> Patrick Lewis, et al. / [원문 링크](https://arxiv.org/pdf/2005.11401)

LLM을 단순히 똑똑한 챗봇이 아니라 실제 업무에 활용할 수 있게 된 것은 RAG의 등장 이후부터일 것이다. 실제 생산성 있는 업무에 생성형 AI를 활용하려면 첫째, 실시간 정보를 이용할 수 있어야 한다. 모델 학습 시점에서 정보가 끊겨버리면 과거의 정보를 현재 시점의 정보인 것 처럼 전달할 수 있다. 둘째, 답변의 근거를 확인할 수 있어야 한다. 기존에 매개변수 메모리만을 활용하는 PLM은 가지고 있는 지식을 수많은 가중치에 분산하여 저장한다. 즉, 정보를 제공할 때에도 그 정보가 어디서 온 것인지 알 수 없다. 따라서 출처를 제공하지 못하고, 이러한 답변을 받은 사람은 답변 내용이 사실인지를 직접 찾아 검증해야만 했다. 셋째, 환각의 발생을 최소화해야 한다. 기존에는 모델이 가지고 있는 가중치를 멋대로 조합하여 사실이 아닌 정보를 그럴듯하게 꾸며내는 환각 현상이 잦았다.   
RAG는 이러한 문제점을 '필요한 지식을 외부 문서에서 검색'한 뒤 답변을 생성하게 함으로써 해결하고자 했다.  

### 핵심 아이디어

논문에서는 BART를 매개변수 메모리로 사용하고, 위키백과 문서의 벡터 인덱스를 비매개변수 메모리로 사용하였다. 검색기인 DPR은 질문과 관련된 문서를 위키백과에서 찾고, 생성기인 BART는 질문과 검색된 문서를 함께 보고 답변을 생성한다. 기존 언어 모델이 자신의 기억만으로 답하는 방식이라면, RAG는 필요한 자료를 찾아보며 답하는 방식이라고 할 수 있다.  

### RAG 구조

1. 질문이 들어오면 검색기가 입력된 질문(x)와 관련된 문서(z)를 찾는다.  
2. 질문(x)과 문서(z)를 인코더(BERT)로 벡터화한다.  
3. 두 벡터의 내적을 구하고, 내적값이 클수록 질문과 문서 사이의 관련성이 높다고 판단한다.  
4. MIPS를 통해 상위 K개의 문서를 검색한다.  
5. 질문(x)과 문서(z)를 생성기(BART)에 넣으면 질문(x), 문서(z), 지금까지 생성된 토큰을 바탕으로 다음 토큰을 생성한다.  

### 검색과 생성의 결합

RAG의 핵심은 검색과 생성을 단순히 순서대로 연결한 것이 아니라, 두 과정을 하나의 확률모형으로 결합했다는 점이다. 검색기는 질문과 각 문서가 관련될 확률을 계산하고, 생성기는 해당 문서를 이용했을 때 정답을 생성할 확률을 계산한다. RAG는 두 확률을 곱하고, 검색된 문서 전체에 대해 합산한다. 검색된 문서 A의 관련성은 높지만 실제 정답은 포함하지 않고, 문서 B의 검색 순위는 낮지만 정확한 정답을 포함할 수 있기 때문에 RAG는 검색 순위만으로 하나의 문서를 선택하지 않고 해당 문서가 실제 정답 생성에 얼마나 도움이 되는지도 함께 고려한다. 또한 학습 과정에서 정답 생성 확률이 높아지는 방향으로 검색기도 함께 조정된다. 따라서 검색기는 단순히 질문과 단어가 비슷한 문서가 아니라, 최종 답변 생성에 도움이 되는 문서를 찾도록 학습된다.  

### RAG-Sequence와 RAG-Token

논문에서는 검색된 문서를 답변 생성에 반영하는 방법으로 RAG-Sequence와 RAG-Token을 제안하였다.  

RAG-Sequence는 하나의 문서가 답변 전체의 생성에 영향을 준다고 가정한다. 상위 K개 문서 각각을 바탕으로 완성된 답변의 생성 확률을 계산한 뒤, 문서의 검색 확률을 반영하여 이를 합산한다. 즉, A 문서로 답변 전체를 만들었을 경우, B 문서로 답변 전체를 만들었을 경우, C 문서로 답변 전체를 만들었을 경우를 각각 평가한 뒤에 합친다.  

반면 RAG-Token은 각 토큰을 생성할 때마다 상위 K개 문서의 영향을 다르게 반영한다. 예를 들어 하나의 답변에서도 첫 번째 내용은 문서 A, 다음 내용은 문서 B의 정보를 더 크게 활용할 수 있다. 다만 토큰마다 문서를 새로 검색하는 것은 아니며, 처음 검색된 상위 K개 문서의 가중치가 토큰마다 달라지는 방식이다.  

즉, RAG-Sequence는 하나의 문서가 답변 전체를 담당하는 방식이고, RAG-Token은 여러 문서가 답변의 각 부분에 나누어 기여할 수 있는 방식이다.  

### 학습

학습 데이터에는 질문과 정답만 주어지며, 어떤 문서를 검색해야 하는지는 직접 제공되지 않는다. 따라서 검색 문서 (z)는 결과에 영향을 미치지만 직접 관찰할 수 없는 잠재변수로 취급된다. 모델은 검색한 문서를 이용해 정답을 얼마나 잘 생성했는지를 기준으로 검색기와 생성기를 함께 학습한다. 다만 문서 인코더를 수정하면 모든 문서 벡터를 다시 계산해야 하므로, 논문에서는 문서 인코더와 문서 인덱스는 고정하였다. 대신 질문 인코더와 BART 생성기만 학습하였다.

### RAG의 의미

앞서 서론에서 제기한 업무에 활용 가능한 LLM의 조건을, RAG는 외부 문서를 참조하는 검색기와 문서 관련성 + 답변 유용성을 확률적으로 곱하는 방식으로 상당 부분 해결하였다. 외부 문서 인덱스를 교체하거나 갱신하면 모델 전체를 다시 학습하지 않고도 지식을 업데이트할 수 있게 된 데다 검색된 문서를 확인할 수 있어 답변의 근거 추적이 가능해졌다. 또한 사실에 기반하여 답변을 생성할 가능성이 높아져 환각도 줄어들었다.  

물론 RAG가 이러한 문제를 완전히 해결한 것은 아니다. 최신 문서가 인덱스에 반영되지 않으면 과거 정보를 검색할 수 있고, 잘못된 문서를 검색하거나 생성기가 문서와는 다른 답변을 만들 가능성도 존재한다. 그럼에도 언어 모델의 생성 능력과 외부 지식을 결합하여 LLM을 실제 업무에 활용할 수 있는 기반을 마련했다는 점에서 의미가 큰 논문이라고 생각한다.  

학원 강의에서 RAG가 무엇인지, 어떻게 사용하는 것인지를 배우긴 했지만 이토록 구체적인 원리는 모르고 실제 프로젝트에서 사용했었다. 하지만 기술을 배울 때는 역시 수학적인 수식을 통해 정확히 어떤 메커니즘으로 작동하는지를 알게 되는게 가장 흥미로운 것 같다. 지금은 여유가 없지만 기회가 된다면 논문 재현도 꼭 해서 기록으로 남겨보고 싶다.