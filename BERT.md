# **BERT: Pre-training of Deep Bidirectional Transformers for Language Understanding(2018)**
> Jacob Devlin, et al. / [원문 링크](https://arxiv.org/pdf/1810.04805)

구글에서 발표한 BERT 모델의 탄생이 담긴 논문이다. 이름에서 알 수 있듯이 트랜스포머 기반 자연어 모델이며, 이 역시 많은 혁신적인 기술을 도입하며 AI 역사의 중요한 축을 차지하고 있다. 트랜스포머와 달리 모델 구조 설명보다는 패러다임 전환의 핵심 개념별로 직관적으로 풀어보고자 한다.

### 양방향(Bidirectional)
모델명 BERT는 Bidirectional Encoder Representations from Transformers로, 즉 트랜스포머 기반 양방향 인코더 표현 모델이다. 트랜스포머의 인코더 부분을 사용하며, 기존 트랜스포머 기반 모델들이 일방향(left-to-right) 혹은 단순 합산 양방향(concatenation of left-to-right and right-to-left) 구조를 띤 것과 차별되게 순수 양방향 추론을 적용한다. 일방향은 말 그대로 어떠한 단어를 추론할 때 앞의 단어를 이용하여 추론하는 것이고, 단순 합산 양방향은 앞쪽 단어와 뒤쪽 단어로부터 각각 추론하고 그 결과를 합산하는 방식이다. 이러한 방식은 문장 전체의 문맥을 고려하기가 불가하거나 또는 불완전하다는 것이다. BERT는 양방향의 단서를 한번에 통합하여 추론하기 위해 MLM(Masked Language Model) 과제를 고안하였다. MLM에서 BERT는 전체 단어 중 15%를 랜덤하게 마스킹 처리한다. 즉, "나는 오늘 아침 밥을 [Mask] 학교에 갔다." 로 문장을 변형시키고 [Mask] 안에 들어갈 단어를 맞추게끔 하는 것이다. 하지만 여기서 BERT는 사전학습 모델로, pre-training 단계와 fine-tuning 단계로 나누어져 있다. 사전학습 시에 MLM을 적용시켜 모든 문장에 [Mask]를 넣어버리면 [Mask]가 존재하지 않는 파인튜닝 단계에서 모델은 추론을 제대로 할 수 없게 된다. 학습 내용과 아예 다른 형태이기 때문이다. 그래서 도입된 것이 80:10:10 마스킹이다. 전체 마스킹(15%) 중 80퍼센트는 [Mask]를 그대로 적용하고, 10퍼센트는 다른 임의의 단어로 교체해버린다("나는 오늘 아침 밥을 밟고 학교에 갔다."). 나머지 10퍼센트는 원래 문장 그대로 유지한다("나는 오늘 아침 밥을 먹고 학교에 갔다."). 쉽게 말해서 AI가 [Mask]가 없더라도 손실함수를 계산하여 문장의 적절함을 평가하고 수정하게끔 함정을 파놓는 것이다. 

### 문장 간의 관계 파악(NSP)
NSP(Next Sentence Prediction)과제는 기존 트랜스포머와 기반 모델들이 문장 내의 관계를 파악하고 각 단어의 의미를 파악하여 문장을 해석했던 것에서 한 발 더 나아가, 문장 뒤에 어떤 문장이 와야 자연스러운지를 파악하고자 고안되었다. 질문-답변, 전제-가설과 같은 둘 이상의 문장이 함께 쌍으로 해석되어야 하는 경우, 기존에는 두 문장을 각각 인코딩한 후 추가로 Cross-Attention 레이어를 얹는 방식으로 해결하였다. 그러나 BERT는 두 문장 사이에 [SEP] 토큰 하나를 추가하는 것으로 하나의 시퀀스로 만든 후, 토큰 임베딩(단어 의미) + 세그먼트 임베딩(소속 문장) + 포지션 임베딩(단어 순서)를 더하였다. 이 결과로 만들어진 인풋 임베딩을 통째로 self-attention을 통과시켜 문장들 간의 관계까지 한 번에 파악할 수 있게끔 하였다. BERT는 문장 사전학습 시 단일 문장 여러개 대신 위키피디아 등을 학습시켜 단락에서 긴 글까지도 흐름을 이해할 수 있게 되었다.

### CLS token
[CLS] 형태로 모든 시퀀스(입력값)의 맨 앞에 반드시 위치하는 토큰은 BERT 모델의 혁신 중 하나이다. 앞서 설명한 양방향 셀프 어텐션을 거치며 추출된 시퀀스의 모든 정보는 CLS 토큰 안에 저장된다. 처음 이 개념을 접했을 때 하나의 토큰에 어떻게 긴 시퀀스의 다양한 특징을 다 담는다는 것인지 의아했지만 CLS 토큰 역시 768차원(BERT Base 기준)의 임베딩 형태이기 때문에 다차원에 풍부한 정보를 담는 것이 가능하다는 것을 깨달았다. 이 CLS 토큰 하나로 인해 기존에 특징 추출을 요약하고자 수행하던 별도의 풀링 등의 과정이 생략되어 모델이 더욱 간소화될 수 있었다.

### 성과 측정 메커니즘
SQuAD(Stanford Question Answering Dataset)란 질문과 본문을 제시하고 본문 속의 특정한 문구를 찾아 답변해야 하는 과제이다. BERT는 S(start), E(end)라는 두 개의 벡터 개념을 도입함으로써 간단히 이 과제를 해결하였다. 간단하게 설명하면 본문 내에서 S와 E의 위치를 찾아내는 방식으로 답을 찾았다.(내적과 손실함수 사용) 이는 기존에 질문을 인코딩하고 본문에서 정답을 찾아 디코더로 한 글자씩 생성하는 등의 방식에 비하면 엄청난 개선이라고 할 수 있다. 또한 SQuAD 2.0에서는 본문에 정답이 없는 함정 문제가 존재하는데, 이 경우 S와 E를 전부 [CLS]에 대응시켜 근거 없는 정답을 지어내는 것이 아니라 정답을 찾을 수 없다는 신뢰성 있는 답변을 하게끔 설계되었다. 

### 과적합(Overfitting) 역설
BERT 이전의 통념은 소형 데이터셋으로 너무 많은 파라미터를 가진 모델을 학습시키면 과적합 문제가 필연적이라는 것이었다. 그러나 약 3600여개의 데이터로 구성된 MRPC로 무려 3억 4,000만 개의 파라미터를 가진 BERT Large를 학습시켜도 과적합 문제가 발생하지 않았다. 이는 BERT가 이미 충분히 사전학습 된 백본을 가지고 있기 때문에 데이터셋이 매우 작더라도 파라미터를 적절하게 조절하여 과적합을 피할 수 있는 것이다. 

### BERT의 의미
BERT는 기존에 용도별로 전용 아키텍처를 만들어 일일이 대응하던 문제들을 제대로 사전학습된 단 하나의 초거대 LLM 모델을 간편하게 튜닝하는 것만으로 해결할 수 있다는 것을 보였다. 나는 이러한 점에서 BERT가 진정한 '인공지능'의 시발점이 아니었을까 하는 생각이 들었다. 기존에도 neural network를 이용한 모델들은 다수 존재하였지만 그들은 어떠한 용도에 맞게 제작된 정교한 기계에 가깝다는 느낌이었다면, BERT부터는 기본적으로 학습된 인공지능이 존재하고 이를 약간의 학습으로 교육시켜 활용한다는 점에서 인간과 유사한 느낌을 받았다. 트랜스포머에서 제안된 혁신들도 놀라웠지만, 트랜스포머에서 필요한 부분만을 채용하고 여러 기술을 녹여내서 거대 LLM의 시초를 만든 BERT 또한 공부할수록 대단하다는 생각이 들었다. 