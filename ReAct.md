# **REACT: SYNERGIZING REASONING AND ACTING IN LANGUAGE MODELS(2023)**
> Shunyu Yao, et al. / [원문 링크](https://arxiv.org/pdf/2210.03629)

최근 AI업계에서 가장 뜨거운 기술이 무엇이냐 하면 당연 에이전트일 것이다. 단순히 질문에 답하는 챗봇을 넘어, 직접 정보 검색부터 프로그램 실행, 업무 수행까지 해내는 AI 에이전트는 기존의 업무 수행 방식을 크게 뒤흔들고 있다. ReAct는 이러한 에이전트 구조의 기반이 된 논문 중 하나로, 이름 그대로 Reasoning + Acting을 결합한 행동 방식을 제안한다.

### 배경

논문에서 ReAct와 자주 비교되는 것이 Chain-of-Thought(CoT), 즉 단계별로 생각하는 추론 모델이다. CoT는 중간 추론 단계를 도입함으로써 복잡한 문제의 해결 능력을 높였지만, 사고의 과정 속에서 외부의 정보를 얻을 수 없는 갇힌 추론이라는 한계가 있었다(정적인 블랙박스). 중간 단계에서 발생한 오류가 뒤의 추론으로 계속 전달되는 오류 전파가 발생하는 것이다. 반면 Act-only, 즉 행동 모델은 직접 검색을 하거나 환경을 조작할 수 있지만, 행동-관찰-행동-관찰의 패턴으로 이루어져 어떤 행동을 해야할지, 얻은 정보로 답을 할 수 있는지 등을 판단하는 사고능력이 떨어지는 문제가 있었다. ReAct는 이 둘을 생각-행동-관찰-생각-행동-관찰의 패턴으로 연결하여 모델이 생각한 뒤 행동하고, 그 결과를 관찰하여 다시 생각하고 행동하는, 인간의 사고와 유사한 방식을 구현했다.

### ReAct 구조

1. Thought: 현재까지의 정보를 정리하고 다음에 해야 할 일을 추론한다.
2. Action: 검색, 문서 조회, 환경 조작 등 행동을 수행한다.
3. Observation: 행동의 결과를 확인한다.

예) Q: Apple Remote가 제어하도록 설계된 프로그램을 제어할 수 있는 Apple Remote 외의 장치에는 무엇이 있어?

    Thought1: Apple Remote 검색해야 해.
    Action1: Apple Remote 검색 실행
    Observation1: Apple Remote는 ...Front Row를 ...

    Thought2: Front Row 검색해야 해.
    Action2: Front Row 검색 실행
    Observation2: Front Row를 찾을 수 없어요. 유사: 'Front Row Seat to Earth', 'Front Row (software)', ...

    Thought3: Front Row가 검색되지 않았네. Front Row (software)로 시도해보자.
    Action3: Front Row (software) 검색 실행
    Observation3: Front Row는 ... Apple Remote나 키보드 fn 키로...

    Thought4: Front Row (software)는 Apple Remote나 키보드 fn 키로 조작할 수 있구나. 그렇다면 답은 키보드 fn 키다.
    Act4: 키보드 fn 키 출력하고 종료

이처럼 ReAct 구조는 실시간으로 행동의 결과를 Observation으로 받고, 이를 토대로 Thought에서 계획을 계속 수정한다. 검색 결과가 나오지 않으면 검색어를 바꾸고, 정보가 충분하지 않으면 추가 행동을 계획한다. 즉, 동적인 추론과정을 거친다.

### 핵심 아이디어

**기존 에이전트의 행동 공간에 언어 공간 추가**
ReAct는 기존 에이전트의 행동 공간에 자유로운 언어로 된 Thought를 추가하였다. Thought는 외부 환경을 직접 변화시키지는 않지만, 현재까지의 정보를 정리하여 문맥을 업데이트하고 이후의 추론과 행동을 돕는다.

**Reason to Act와 Act to Reason**
Reason to Act는 추론을 통해 적절한 행동을 선택하는 것이다. 복잡한 목표를 작은 단계로 나누고, 필요한 정보를 판단하고, 어떤 도구를 사용할지 결정한다. 추론 과정이 없다면 에이전트가 이전에 했던 행동을 의미 없이 반복하거나 목표와 관련없는 행동을 하기 쉽다.
Act to Reason은 행동을 통해 추론에 필요한 새로운 정보를 얻는 것이다. 모델 내부의 기억만으로는 알 수 없는 사실을 검색하거나 실제 환경에서 물건이 존재하는지 확인하고, 프로그램을 실행한 결과를 관찰한다. 이렇게 얻은 정보는 다음 Thought의 근거가 된다.
이러한 Reason과 Act의 폐쇄루프 반복구조가 ReAct의 핵심이 된다.

### 성능 (지식 검색)

논문에서 ReAct의 지식 추론 능력을 평가하기 위해 HotpotQA와 FEVER 과제를 수행하였다. 모델에는 질문만 주어지고, 외부 지식을 얻기 위해 사용할 수 있는 도구로 위키피디아 API가 제공된다.

행동은 다음 세 가지로 단순하게 구성된다.
- Search[검색어]: 특정 위키피디아 문서를 검색한다.
- Lookup[문구]: 현재 문서에서 특정 문구가 포함된 내용을 찾는다.
- Finish[정답]: 최종 답을 제출한다.

모델은 먼저 질문을 여러 단계로 분해하고 첫 번째 검색 대상을 선택한다. 검색 결과에서 필요한 정보를 추출한 뒤, 그 정보로부터 다음 검색 대상을 결정한다. 충분한 근거가 모이면 최종 정답을 제출한다. 검색과 생성이 한 번씩만 이루어지는 전형적인 단일 검색 RAG와 비교하면, ReAct는 검색 결과를 보고 다음 검색을 스스로 결정하는 반복적인 검색 구조라는 차이가 있다.

실험에서 ReAct는 외부 지식을 사용하기 때문에 CoT보다 사실에 근거한 추론을 하는 경향을 보였으며, 환각도 상대적으로 적게 발생하였다. 그러나 항상 CoT보다 높은 성능을 보인 것은 아니다. ReAct는 검색 결과가 부정확하면 전체 추론이 흔들렸고, 같은 검색과 생각을 반복하는 루프에 빠지기도 했다. 반면 CoT는 외부 사실을 지어낼 위험은 크지만, 모델 내부에서 자유롭게 추론 구조를 만들 수 있다는 장점이 있었다.

논문에서는 두 방식 중 하나만 고집하지 않고, ReAct가 제한된 단계 안에 답을 찾지 못하면 CoT를 사용하거나 CoT의 답변들이 서로 일치하지 않을 때 ReAct로 사실을 확인하는 결합 방식도 제안하였다. 모델 내부의 지식과 외부에서 확인한 지식은 서로 대체하는 관계가 아니라 보완하는 관계라는 것이다.

### 성능 (의사결정)

ReAct는 정보 검색뿐만 아니라 실제 환경에서 여러 행동을 수행해야 하는 과제에도 적용되었다. ALFWorld는 가상의 집 안에서 물건을 찾고 옮기는 텍스트 기반 환경이다. 예를 들어 “후추통을 찾아 서랍에 넣어라”라는 목표가 주어지면 에이전트는 여러 방과 가구를 돌아다니며 후추통을 찾고, 서랍을 연 뒤 물건을 넣어야 한다.

Act-only 모델은 눈앞의 상황에 반응하여 행동은 생성하지만, 전체 목표를 작은 단계로 나누거나 완료된 작업을 기억하지 못했다. 물건을 이미 찾았는데도 계속 다른 장소를 탐색하거나, 닫힌 서랍에 물건을 넣으려다 실패한 행동을 반복하는 문제가 발생하였다.

ReAct는 “먼저 후추통을 찾아야 한다”, “후추통을 찾았으므로 이제 서랍으로 가야 한다”와 같은 Thought를 중간에 생성하였다. 이 Thought는 긴 작업 과정에서 현재 상태를 요약하고 다음 목표를 지정하는 체크포인트 역할을 한다. 모든 행동마다 생각을 생성할 필요는 없으며, 목표를 나누거나 계획을 전환해야 하는 중요한 시점에서만 드물게 Thought를 생성해도 효과가 있었다. 지식 검색 과제에서는 거의 매 행동마다 추론하는 dense reasoning을 사용했지만, 긴 의사결정 과제에서는 필요한 시점에만 추론하는 sparse reasoning을 사용한 것이다.

WebShop에서는 사용자의 조건에 맞는 상품을 검색하고 옵션을 선택하는 과제를 수행하였다. 에이전트는 상품명, 가격, 크기, 색상 등 여러 정보를 읽은 뒤 어떤 상품이 사용자의 요구조건을 만족하는지 판단해야 했다. ReAct는 검색 결과에서 중요한 조건을 추출하고, 추가 검색이 필요한지 또는 상품을 선택해도 되는지를 추론함으로써 행동만 생성하는 방식보다 높은 성능을 보였다. 이는 ReAct가 단순한 질의응답을 넘어 웹 탐색과 업무 수행에도 적용될 수 있음을 보여준다.

### 한계

- ReAct는 구조가 Thought-Action-Observation으로 정해져 있어 CoT보다 추론의 자유도가 낮을 수 있다. 
- 검색이나 행동이 실패했을 때 같은 Thought와 Action을 반복하며 루프에 빠지는 오류가 발생할 수 있고 검색 결과가 부정확하거나 비어 있으면 이후 추론도 잘못된 방향으로 진행될 위험도 있다. 
- 모델이 작거나 과제가 복잡한 경우, 적은 예시(few-shot)으로는 추론과 행동을 제대로 학습하지 못할 수 있다. 따라서 더 많은 학습 데이터와 파인튜닝, 강화학습과 결합될 필요가 있다. 
- 스스로 생각해서 행동하기 때문에 모델이 외부 환경과 연결되어 있다면 예측하지 못한 행동을 할 위험도 존재한다.

### ReAct의 의미

앞선 한계점에도 불구하고 ReAct는 새로운 모델 구조를 만들지 않고 현대 AI Agent의 기반을 만들었다는 점에서 큰 의미가 있다. 사전학습된 언어 모델(PLM)의 가중치를 수정하지 않고 사람이 작성한 몇 개의 Thought -> Action -> Observation 예시와 행동 결과를 다시 모델에 전달하는 간단한 상호작용 구조만으로 의미 있는 결과를 얻어낸 것이다. 즉 ReAct는 언어 모델을 사용하는 방법을 제시한 것으로 볼 수 있다. 

언어 모델에게 사람과 유사한 생각-행동-관찰의 패턴을 '알려준' 것만으로도 모델이 기존의 질문에 답을 생성하는 모델을 넘어 업무를 대신 수행할 수 있는 수행자로 한 차원 발전했다는 사실이 매우 놀라웠다. 기존에 Agentic workflow를 구성하고 활용하면서도 어떻게 자체적으로 판단해서 행동한다는 것인지, 어떤 기술이 적용된 것일지 궁금했고 그래서 ReAct 논문을 읽어보게 된 것이었다. 막상 읽어보니 수학적 수식은 에이전트의 행동 과정을 형식화한 것이었고, 새로운 모델 구조보다 모델이 어떤 순서로 생각하고 행동하며 그 결과를 다시 받아들이도록 설계하느냐가 핵심이었다. 고도의 모델 기술만이 AI의 발전을 만드는 것은 아니며, 기존 모델과 도구를 어떻게 연결하고 활용할지에 관한 아이디어 역시 중요하다는 점이 인상 깊었다. 기술적 역량만을 계속 키우려 했던 내 모습을 돌아보고, 지금 가지고 있는 지식과 도구를 어떻게 새롭게 연결할 수 있을지 먼저 생각해봐야겠다고 느꼈다.