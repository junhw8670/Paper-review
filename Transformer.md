# **Attention Is All You Need (2017)**
> Ashish Vaswani, et al. / [원문 링크](https://proceedings.neurips.cc/paper/2017/file/3f5ee243547dee91fbd053c1c4a845aa-Paper.pdf)

발표 이래로 10년 가까이 지난 지금까지도 수많은 LLM의 표준 아키텍처로 활용되는 트랜스포머 모델이 첫 등장한 논문이며, 이 논문을 빼놓고는 AI를 얘기할 수 없을 정도로 매우 유명한 논문이다.
이 혁신적인 논문에 대해 내가 이해한 바를, 핵심 개념을 최대한 직관적으로 풀어 쉽게 설명해보는 차원으로 리뷰를 해보고자 한다.
트랜스포머 이후 비전 트랜스포머가 고안되고 여러 개선·변형이 이루어져 현재의 LLM이 탄생하였으나, 그 시작은 영어-독어 번역 성능을 끌어올리는 것이었다. 기존에도 자연어를 해석하고 번역하는 모델, 즉 RNN(Recurrent Neural Networks)이나 LSTM(Long Short-Term Memory)과 같은 단일 모델 혹은 여러 단일 모델들을 결합하여 성능을 끌어올린 앙상블 모델들이 존재하였다. 이 모델들은 쉽게 말해 문장의 단어 하나하나를 기억하여 다음 단어를 예측하고 생성한다. 반면 트랜스포머가 혁신적이었던 부분은 문장의 단어들이 서로 간의 관계를 단 한번에 인식할 수 있다는 것이다. 여기서 사용되는 기술이 바로 self-attention인데, 이를 이해하기 위해서 우선 임베딩에 대한 이해가 필수적이다. 
임베딩이란, 숫자밖에 모르는 컴퓨터에게 문맥을 이해시키기 위해 자연어를 숫자로 바꾸는 것이다. 자연어를 숫자로 1대 1 대응시키는 대신, 단어를 다차원의 숫자로 넓게 펼친다고 생각하면 된다. 예를 들어, "나는 밥을 먹는다."라는 문장은 512차원에 임베딩되면 각 단어별로 512개의 숫자를 갖는다. 나는 = [-1, 0.41, 0.032, 0.6, 0.7, ...], 밥을 = [0.7, 0.78, 3.45, -0.7, 0, ...], 먹는다 = [0.2, 0.4, 0.56, 0.003, -0.09, ...]. 처음에는 랜덤으로 숫자가 할당되지만, 학습을 통해 공간벡터에서 의미에 맞는 위치를 점차 찾아가게 된다. 개는 강아지, 고양이와 가까운 위치에 놓이고 자동차, 타다와는 먼 거리에 놓이게 된다. 이러한 위치조정 끝에 의미를 담은 임베딩이 완성된다. 
self-attention은 Q(query), K(key), V(value)의 전치와 행렬곱으로 이루어지는데, 여기서 Q와 K, V는 문장 자기 자신으로 같다(self). 즉, 앞서 세 단어를 숫자로 만든 행렬이 그대로 Q, K, V가 된다. 그렇지만 3 x 512 행렬이 통째로 들어가는 것이 아니라 몇 개로 쪼개져서 들어가게 된다. 3 x 512 행렬을 8개로 쪼개면 3 x 64 행렬이 8개 만들어지고, 이는 8개의 head를 가진 Multi-Head Attention 모델이다. 이 Multi-Head Attention은 self-attention이 너무 많은 특징이 한번에 합쳐져 특징이 뭉그러지는 현상을 해결하기 위해 고안된 것으로, 각 헤드가 단어의 다른 특징에 집중하게 한다. 예를 들어, 1번 헤드는 옆 단어와의 관계, 2번 헤드는 그 단어가 가리키는 대상에 주목하는 식이다. 이런 식으로 단어가 가진 다양한 특징을 모두 추출하여 풍부한 해석이 이루어질 수 있다.
Q는 3 x 64 그대로, K는  3행 64열에서 64행 3열이 되게끔 전치시킨다(Kt). 그러면 Q의 열과 K의 행이 일치하게 되므로 행렬곱을 적용할 수 있게 된다. 그 결과는 3 x 3의 표 형태로 나타나게 되는데, 이는 문장 내의 단어들끼리(자기 자신 포함)의 연관성이 담긴 표이다. 이 표의 값을 지나치게 커지는 것을 막기 위해 스케일링을 거친 후 softmax 함수로 처리하면 숫자가 0에서 1 사이의 연관도 점수로 예쁘게 변환된다. 이는 다시 V(3 x 64)와 행렬곱되어 3 x 64의 결과행렬이 도출된다. 여기서 Q와 Kt를 행렬곱하여 연관성이 담긴 표를 만드는 것은 각 단어가 다른 단어(혹은 자기 자신)와 어떤 관계인지를 알아보고자 하는 것이고, 마지막 V는 그 관계 있는 단어가 어떤 단어인지 그 원본 정보를 추가하기 위해 필요한 것이다. 이를 그림으로 표현하면 다음과 같다.
![alt text](image.png)
![alt text](image-1.png)
이처럼 self-attention을 통해 문장 자체에서 단어의 의미를 유추할 수 있는 것인데, 이는 긴 문장 안에서 단어 간 거리와 상관없이 관계를 기억할 수 있어 연산이 간편하고 성능을 향상시킨다. 무엇보다 병렬 처리가 가능하다는 것이 엄청난 장점이다. 기존 기억 모델들이 긴 문장의 첫 단어부터 끝 단어까지 하나하나를 기억하며 계산해야 하고 가는 도중에 가중치가 희석될 위험이 있었던 문제를 해결한 것이다. 
임베딩과 self-attention 사이에는 positional encoding이라는 과정이 존재하는데, 이는 문장의 모든 단어를 한 번에 다루는 self-attention의 특성 상 문장 내 단어의 순서 정보를 유지하기 위해 고안된 장치이다. sin cos 함수를 홀수·짝수로 배분하여 파동으로 위치정보를 저장하는 방법과 일반적인 임베딩 학습으로 위치 정보를 기억하는 방식이 존재하는데, 둘 간의 큰 성능 차이는 없으나 학습된 문장보다 더 긴 문장이 등장할 경우 확장성을 위해 고정된 sin cos 방법을 채택하였다고 저자는 말한다. 이로써 단어 간의 의미관계와 순서까지 파악할 수 있게 되었다.
어텐션 과정을 거친 결과는 원본 정보가 유실되는 것을 방지하기 위해 원본 정보와 더해지고(잔차 연결) 레이어 정규화 과정을 거친다. 
![alt text](image-2.png)
여기까지가 트랜스포머 인코더의 두 서브레이어 중 첫번째 레이어까지의 과정이다. 다음 서브레이어는 Feed-Forward Networks 레이어인데, 이곳에서는 두번의 선형 변환 사이에 ReLU 함수를 포함한다. 기본 트랜스포머 모형의 경우 512차원을 2048차원으로 확장하고 ReLU로 비선형성을 표현한 뒤 다시 512차원으로 축소하는 과정을 거친다. (ReLU: y = max(0, x)) 문장 내의 단어들은 모두 같은 변환을 거치지만, 레이어마다는 각각의 가중치를 가질 수 있다. 여기서는 문장 내 단어 간의 관계가 아닌 단어 자체의 의미에 집중하여 특징을 추출한다. 이전 어텐션 서브레이어와 마찬가지로 피드포워드 서브레이어도 원본 정보와 더해지는 잔차 연결, 그리고 레이어 정규화 과정을 거친다.
이러한 두 서브레이어로 이루어진 인코더가 여러 층 존재하며, 기본형 트랜스포머는 6층으로 이루어져 있다.
디코더도 마찬가지로 6층으로 이루어지며, 인코더와 같이 임베딩과 포지셔널 인코딩을 거친 후, 인코더에는 없는 서브레이어가 등장한다. Masked Multi-Head Attention이라고 불리는 이 서브레이어는 역시 잔차연결과 레이어 정규화 과정이 뒤따르며, 인코더의 self-attention과 유사하지만 정답을 생성하는 과정이기 때문에 뒤에 만들어질 단어를 미리 보게 되는 것을 방지하는 마스킹 처리가 추가된다. 
두 번째 서브레이어는 멀티헤드 어텐션 + 잔차 연결 + 정규화이다. 이 멀티헤드 어텐션에서는 앞선 self-attention과는 다소 다른 Encoder-Decoder Attention 방식이 적용된다. Q, K, V에서 Q는 디코더의 이전 서브레이어가 처리한 현재까지 생성된 문장 정보를 받고 K, V는 인코더에서 전달된 인풋에 대한 정보가 된다. 따라서 이 서브레이어는 다음으로 만들어야 할 단어를 인풋 정보를 기반으로 결정할 수 있다.
마지막 서브레이어는 인코더와 똑같은 Feed-Forward Network 레이어이며, 이후 선형 변환과 softmax를 한 번씩 거쳐 최종 아웃풋이 만들어진다.

트랜스포머를 state of the art 모델이 된 데에는 많은 뛰어난 기술들이 사용되었지만, 핵심은 논문 제목에서 알 수 있듯이 attention이라고 할 수 있다. 여기서 내가 특별하게 생각한 것은 attention이라는 기법이 이전에 없던 전혀 새로운 무언가가 아니었다는 것이다. 기존의 모델들에서도 문장 내 단어에 대한 집중도를 파악하고자 할 때 attention 기법을 통상적으로 사용하고 있었는데, 이러한 보조기법에 불과하던 attention의 가치를 알아보고 attention만으로 모델을 만들어 기존의 SOTA를 뒤엎은 것이 정말 놀랍다고 생각했다. 
